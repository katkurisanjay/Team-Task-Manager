{
  "name": "team-task-manager",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cd backend && npx prisma generate",
    "start": "cd backend && npx prisma db push --accept-data-loss && node src/server.js"
  }
}
